import React from 'react';

const Footer = ({ homeRef, aboutRef, facilitiesRef, locationRef, contactRef, scrollToSection }) => {

    return (

        <div>
            <strong><p id="copyright-notice"></p></strong>
        </div>
    );
};

export default Footer;
